version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22 && node --version
        - npm ci
    build:
      commands:
        - NITRO_PRESET=aws_amplify npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
